import { CHAIN_ID } from "../src";
import { getTokenInfo } from "./../src/token";

describe("GET TOKEN", () => {
  it("get token info", async () => {
    const tokenAddresses = "0xDcf0852Ae735A62d45090e89668AD6b358419Ccc";
    const token = await getTokenInfo(tokenAddresses, CHAIN_ID);
    console.log(token);
  });
});
