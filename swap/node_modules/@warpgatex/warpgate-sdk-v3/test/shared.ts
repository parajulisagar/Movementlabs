import { CHAIN_ID, RPC_URL } from "./../src/constants";
import { ethers } from "ethers";

export const provider = new ethers.providers.JsonRpcProvider(RPC_URL[CHAIN_ID]);
export const secretKey = process.env.PRIVATE_KEY || "";
export const signer = new ethers.Wallet(secretKey, provider);
