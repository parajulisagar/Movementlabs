import { JsonRpcProvider } from '@ethersproject/providers';
import { ChainId } from '@warpgatex/warp-sdk-core';
import { SwapOptions, SwapRoute } from '../routers';
import { IPortionProvider } from './portion-provider';
import { ProviderConfig } from './provider';
import { Simulator } from './simulation-provider';
import { ArbitrumGasData, OptimismGasData } from './v3/gas-data-provider';
import { IV3PoolProvider } from './v3/pool-provider';
export declare class EthEstimateGasSimulator extends Simulator {
    v3PoolProvider: IV3PoolProvider;
    private overrideEstimateMultiplier;
    constructor(chainId: ChainId, provider: JsonRpcProvider, v3PoolProvider: IV3PoolProvider, portionProvider: IPortionProvider, overrideEstimateMultiplier?: {
        [chainId in ChainId]?: number;
    });
    ethEstimateGas(fromAddress: string, swapOptions: SwapOptions, route: SwapRoute, l2GasData?: ArbitrumGasData | OptimismGasData, providerConfig?: ProviderConfig): Promise<SwapRoute>;
    private adjustGasEstimate;
    protected simulateTransaction(fromAddress: string, swapOptions: SwapOptions, swapRoute: SwapRoute, l2GasData?: OptimismGasData | ArbitrumGasData | undefined, _providerConfig?: ProviderConfig | undefined): Promise<SwapRoute>;
}
